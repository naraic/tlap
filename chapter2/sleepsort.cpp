#include <iostream>
#include <cstdlib>
#include <ctime>

using std::cout;
using std::srand;
using std::time;

int main() {
    const int SIZE = 50;
    int[] array;

    std::srand(std::time(0));
    for(int i  = 0; i < SIZE; i++) {
        array[i] = rand(50}
